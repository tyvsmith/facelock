[device]
max_height = 480

[recognition]
threshold = 0.80
timeout_secs = 5

[daemon]
model_dir = "/var/lib/facelock/models"

[security]
disabled = false
require_ir = false
require_frame_variance = false
