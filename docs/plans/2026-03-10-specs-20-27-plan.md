# Execution Plan: Specs 20-27

## Execution Order

```
27 (workspace cleanup)  →  23 (unified binary)  →  20, 21, 22 (parallel)  →  24, 25, 26 (parallel)
```

**Rationale:**
- Spec 27 first: mechanical, unblocks cleaner diffs for everything else
- Spec 23 next: restructures binaries, which 20/24/26 depend on
- Specs 20/21/22 are independent config additions (parallel)
- Specs 24/25/26 are independent CLI subcommands (parallel, after 23)

## Spec 27: Workspace Cleanup (5 min)
- Single agent, sequential
- Edit root Cargo.toml, then all 10 crate Cargo.tomls
- `cargo build --workspace && cargo test --workspace`

## Spec 23: Unified Binary (45 min)
- Single agent, sequential (touches many files)
- Move daemon main loop into CLI `daemon` subcommand
- Move auth oneshot into CLI `auth` subcommand
- Move bench into CLI `bench` subcommand
- Update PAM module, systemd units, PKGBUILD, justfile, docs
- Run all container tests

## Specs 20, 21, 22 (parallel, 15 min each)
- 3 agents in worktrees
- Spec 20: PAM auth_bin config
- Spec 21: Model selection config + manifest SHA hashes
- Spec 22: GPU provider config + ORT session changes

## Specs 24, 25, 26 (parallel, 15 min each)
- 3 agents in worktrees
- Spec 24: `visage setup --systemd`
- Spec 25: `visage tpm status`
- Spec 26: `visage setup --pam`

## Verification

After all specs:
1. `cargo build --workspace && cargo test --workspace && cargo clippy --workspace -- -D warnings`
2. `just test-pam` — container smoke tests
3. `just test-integration` — daemon end-to-end
4. `just test-oneshot` — daemonless end-to-end
5. Version appears once in root Cargo.toml
6. Single `visage` binary handles all subcommands
